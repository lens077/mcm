# MCM

面向开发者与产品经理的桌面端项目规划工具。写下结构化大纲，得到经校验的规划视图，
并导出为 **XMind 与 Visio 里可以继续编辑**的文件。

> 状态：功能实现完整（63/63 计划任务），自动化质量门全绿。
> 两种导出的真实互操作性与 Windows 平台尚待人工验证 —— 见 [HANDOVER.md](./HANDOVER.md) §6。

## 它解决什么

规划工具往往二选一：要么是纯文本清单（可 diff 但看不出结构），要么是图形工具
（好看但数据锁在私有格式里）。MCM 两边都要：

- **输入即文件**：`.mcm` 的正文就是你写的大纲语法，可手工编辑、可进 Git、行级 diff 稳定
- **四种联动视图**：任务分解、依赖网络、时间线、里程碑 —— 同一模型的四种投影
- **导出可再编辑**：XMind 的依赖是真实连线，Visio 的连接线拖动形状时保持粘连；
  不是位图，不是不可编辑的替身

## 快速开始

```bash
make install    # 安装依赖
make dev        # 启动桌面应用
```

没装 `make`（例如 Windows）时用等价的 pnpm 命令：

```bash
pnpm install
pnpm tauri dev
```

运行 `make` 查看全部命令。提交前跑 `make gate`。

在左侧粘贴：

```text
%mcm 1
%title 移动端改版
%start 2026-09-01

- 需求阶段 #t1 @王芳
  - [x] 用户访谈 #t2 [2026-09-01..2026-09-05]
  - 竞品分析 #t3 [3d] <-t2
- 设计阶段 #t4
  - 交互稿 #t5 [5d] <-t3
! 需求冻结 #m1 [2026-09-30] <-t5
```

点「生成规划」。把 `<-t2` 改成 `<-t5` 造个环，看问题面板如何定位。

## 技术构成

| 层 | 选型 | 说明 |
|----|------|------|
| 桌面壳 | Tauri 2.11 | 安装包 3.3 MB，冷启动 P95 25 ms |
| 核心 | Rust | 解析、校验、布局、导出全在这里；前端不持有模型状态 |
| 前端 | React 19 + TypeScript strict | Canvas 2D 自研渲染器，1000 任务保持流畅 |

## 质量门

CI 在 macOS + Windows 双平台执行，全部为合入门槛：

| 门槛 | 当前 |
|------|------|
| Rust 测试（单元 / 属性 / 契约） | 409 通过 |
| 前端测试 | 76 通过 |
| Clippy `-D warnings` | 零警告 |
| 性能预算（7 项） | 全部达标 |
| 端到端场景 | 9/9 |
| 冷启动 P95 | 25 ms（预算 2000 ms） |
| 安装包体积 | 11.3 MB（预算 25 MB） |

## 下载

[Releases](https://github.com/lens077/mcm/releases) 提供 macOS（`.dmg`）与
Windows（`.msi` / `.exe`）安装包，附 `SHA256SUMS` 可校验。
`dev` 为随 main 滚动更新的开发版。

## 文档

- **[HANDOVER.md](./HANDOVER.md)** — 交接文档：架构决策、测试策略、未验证部分、后续方向
- [项目宪法](./.specify/memory/constitution.md) — 七条不可协商的原则
- [规格与契约](./specs/001-project-planning-tool/) — 需求、技术方案、5 份接口契约、格式调研

## 许可

MIT
