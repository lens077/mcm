# 发布人工验收清单

**用途**: 自动化门槛之外、必须由人在真实应用中确认的项（quickstart.md §发布前人工验收）。
**执行**: 每个候选版本在 **macOS 与 Windows 各走一遍**；两平台均全绿才可发布（宪法 I）。

**版本**: `________`　**日期**: `________`　**执行人**: `________`　**平台**: ☐ macOS　☐ Windows

---

## 0. 自动化门槛（发布前必须已绿）

这些由 CI 覆盖，此处只做发布前复核，不逐项手测。

| 门槛 | 命令 | 状态 |
|------|------|------|
| Rust 全量测试（含契约测试） | `cargo test --workspace` | ☐ |
| Clippy 零警告 | `cargo clippy --workspace --all-targets -- -D warnings` | ☐ |
| 格式 | `cargo fmt --all -- --check` | ☐ |
| 性能预算 | `cargo bench -p mcm-core` | ☐ |
| 前端测试 / lint / 构建 | `pnpm test` · `pnpm lint` · `pnpm build` | ☐ |
| 场景冒烟（本平台） | macOS: `pnpm smoke:mac`；Windows: `pnpm e2e:win` | ☐ |
| 冷启动 P95 ≤ 2s | `node scripts/measure-startup.mjs` | ☐ |
| 安装包 ≤ 25MB | `pnpm tauri build && pnpm check:bundle` | ☐ |

---

## 1. XMind 导出真实互操作（SC-004）

自动化只能验证结构与 schema；**能否在 XMind 里继续编辑必须人工确认**。

准备：用 `quickstart` 示例大纲生成规划（含日期、负责人、依赖、里程碑、中文与 emoji）。

| # | 步骤 | 期望 | 状态 |
|---|------|------|------|
| 1.1 | 导出 `.xmind` | 导出成功，摘要列出降级项 | ☐ |
| 1.2 | 在 **XMind 当前稳定版** 中打开 | 无「文件损坏」提示，正常打开 | ☐ |
| 1.3 | 检查层级 | 任务树层级与顺序和应用内一致 | ☐ |
| 1.4 | 检查依赖 | 依赖显示为**可见连线**（非文本） | ☐ |
| 1.5 | 检查里程碑 | 「里程碑」分支存在，节点带红旗标记与日期 | ☐ |
| 1.6 | 检查中文与 emoji | 文本完整无乱码 | ☐ |
| 1.7 | **编辑节点文本** | 可直接改字，非图片 | ☐ |
| 1.8 | **拖动调整结构** | 节点可拖拽换位 | ☐ |
| 1.9 | **保存** | 保存无报错 | ☐ |
| 1.10 | 关闭后重开 | 修改保留，文件正常 | ☐ |

**XMind 版本**: `________`

---

## 2. Visio 导出真实互操作（SC-005）

| # | 步骤 | 期望 | 状态 |
|---|------|------|------|
| 2.1 | 导出 `.vsdx` | 导出成功，摘要列出降级项 | ☐ |
| 2.2 | 在 **Visio 2016+** 中打开 | **无修复对话框**，正常打开 | ☐ |
| 2.3 | 清点形状 | 任务形状数量与规划一致（无静默丢失） | ☐ |
| 2.4 | **拖动一个任务形状** | **连接线跟随移动、保持粘连** ← 本项最关键 | ☐ |
| 2.5 | 检查里程碑 | 显示为菱形，文本含名称与日期 | ☐ |
| 2.6 | **编辑形状文本** | 可直接改字 | ☐ |
| 2.7 | **另画一条连接线** | 可用 Visio 自带连接器连接两个任务形状 | ☐ |
| 2.8 | **保存** | 无损坏警告 | ☐ |
| 2.9 | 关闭后重开 | 修改保留，无修复提示 | ☐ |
| 2.10 | 对 Visio 再保存的文件执行 unzip diff | 记录其归一化差异，回填 golden fixture | ☐ |

**Visio 版本**: `________`　**diff 记录**: `________`

---

## 3. 界面与无障碍（宪法 III）

| # | 检查项 | 期望 | 状态 |
|---|--------|------|------|
| 3.1 | 浅色主题四视图截屏 | 元素清晰、对比度足够 | ☐ |
| 3.2 | 深色主题四视图截屏 | 同上，无「浅色残留」色块 | ☐ |
| 3.3 | 主题切换 | 即时生效，无需重启 | ☐ |
| 3.4 | **纯键盘走一遍主流程** | 新建 → 录入 → 生成 → 切视图 → 撤销 → 保存 → 导出，全程不碰鼠标 | ☐ |
| 3.5 | 快捷键速查（`⌘/` 或 `Ctrl+/`） | 打开正常，列出的组合与实际一致 | ☐ |
| 3.6 | 焦点可见性 | Tab 遍历时焦点环始终可见 | ☐ |
| 3.7 | 动效 | 有限、不干扰；系统「减少动态效果」开启时动效消失 | ☐ |

---

## 4. 平台惯例与文件行为（宪法 I、spec FR-016）

| # | 检查项 | 期望 | 状态 |
|---|--------|------|------|
| 4.1 | 窗口控制 | 最小化/最大化/关闭符合本平台惯例 | ☐ |
| 4.2 | 修饰键 | macOS 用 ⌘、Windows 用 Ctrl | ☐ |
| 4.3 | 文件对话框 | 为系统原生对话框，`.mcm` 过滤器生效 | ☐ |
| 4.4 | 未保存关窗 | 弹出提示，**保存/放弃/取消三选**均按预期工作 | ☐ |
| 4.5 | 外部修改 | 在外部编辑器改动文件后切回应用，提示重新加载；选「保留内存版本」不被覆盖 | ☐ |
| 4.6 | 手工编辑 `.mcm` | 用文本编辑器修改后重开，改动生效 | ☐ |
| 4.7 | 损坏文件 | 故意写坏一行后打开，其余内容仍在，坏行进入问题面板 | ☐ |
| 4.8 | 最近文件 | 重启应用后最近文件列表保留 | ☐ |

---

## 5. 规模与手感（SC-002）

| # | 检查项 | 期望 | 状态 |
|---|--------|------|------|
| 5.1 | 打开 1,000 任务夹具 | `crates/mcm-core/fixtures/perf/plan-1000.mcm` 正常打开 | ☐ |
| 5.2 | 四视图缩放平移 | 无可感知卡顿（开发构建可开帧率浮层佐证） | ☐ |
| 5.3 | 该规模下编辑 | 改名/拖拽/连线反馈即时 | ☐ |
| 5.4 | 该规模下导出两种格式 | 均在数秒内完成且产物可打开 | ☐ |

---

## 结论

☐ **通过** — 两平台全部项已确认，可以发布
☐ **不通过** — 阻塞项：`________________________________`

**签署**: `________`　**日期**: `________`

> 发现的任何缺陷：先补一个能复现它的自动化测试，再修复（宪法 VII）。
